{
    "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
    "$docs": "https://code.claude.com/docs/en/keybindings",
    "bindings": [
        {
            "context": "Chat",
            "bindings": {
                "space": null, // by default it accepts auto compl. suggestions
                "shift+enter": "insertNewline" // break line instead of sending
            }
        }
    ]
}