return {
  {
    "folke/noice.nvim",
    opts = {
      cmdline = {
        view = "cmdline", -- classic bottom cmdline instead of the popup
      },
      presets = {
        command_palette = false, -- don't reposition the cmdline into a centered palette
      },
    },
  },
}
